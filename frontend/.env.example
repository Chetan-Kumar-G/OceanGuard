# Only needed in production, where the frontend and backend are on different
# origins. In dev, leave unset — Vite's proxy (vite.config.ts) forwards
# /api, /events, /f5, /f6, /health to http://127.0.0.1:8000.
VITE_API_BASE_URL=
